<link rel='stylesheet' href="/css/style.css"/>
<h1>Look back on some of your favorite walk experiences!</h1>
<p>Check back on old comments, make edits, walk again, and connect with other users who also walked these pups!</p>
<div class="container-fluid d-flex flex-wrap justify-content-evenly">
    <% animal.forEach(animal => { %>
        <div class="card my-3 my-cards" style="width: 18rem; height: 30rem;">
            <img src= "<%= animal.photos %>" class='card-img-top dog-imgs'/>
            <div class="card-body">
                <h5 class="card-title"><%= animal.name %></h5>
                <div class="d-flex justify-content-evenly">
                    <a href="/pets/<%= animal.id %>" class="btn butts">About Me!</a>
                    <form method="POST" action="/pets/favorites/<%= animal.id %>/?_method=DELETE">
                        <input hidden value="<%= animal.id %>" name='id'/>
                        <button class='btn butts-scary' type="submit">Delete!</button>
                    </form>
                </div>
            </div>
        </div>
    <% }) %>
</div>

